import { Button } from '@mui/material';

const AddAnimeButton = ({anime}) => {
  // TODO : IMPLEMENT A MODAL TO OPEN UP - 
  // .  The modal will then do the POST request based on input setDatas
  return (
    <>
      <Button 
        variant='contained'
        onClick={() => {}}
      >
        Add anime
      </Button>
    </>
  );
}

export default AddAnimeButton;